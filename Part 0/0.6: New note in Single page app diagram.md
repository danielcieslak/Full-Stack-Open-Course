```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    server-->>browser: HTTP status 201 Created
    deactivate server

    Note right of browser:URL redirect, with which the server asks the browser to do a new HTTP GET request 

    ```
